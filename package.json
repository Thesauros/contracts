{
  "name": "thesauros-rebalancer",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:arbitrum": "hardhat deploy --network arbitrumOne --tags usdc-vault",
    "deploy:ink": "hardhat deploy --network ink --tags ink-usdt0-vault",
    "test:fork:ink": "forge test --match-contract AaveV3InkProviderTests --fork-url $INK_RPC_URL -vv"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-foundry": "^3.0.0",
    "@nomicfoundation/hardhat-toolbox": "^7.0.0",
    "dotenv": "^16.4.4",
    "hardhat": "3.2.0",
    "hardhat-deploy": "^2.0.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0"
  }
}